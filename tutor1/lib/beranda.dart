import 'package:flutter/material.dart';

class HalBeranda extends StatelessWidget {
  const HalBeranda({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Scaffold(body: Text("Halaman Beranda")));
  }
}
